- name: Install common base packages
  ansible.builtin.apt:
    update_cache: true
    name:
      - ca-certificates
      - curl
      - gnupg
      - lsb-release
      - python3-pip
      - unzip
    state: present

