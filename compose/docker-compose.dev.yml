services:
  api:
    environment:
      TEACHER_AUTH_USERNAME: ""
      TEACHER_AUTH_PASSWORD: ""

  qdrant:
    ports:
      - 127.0.0.1:${TEACHER_QDRANT_PORT:-6333}:6333

  ollama:
    ports:
      - 127.0.0.1:${TEACHER_OLLAMA_PORT:-11434}:11434
